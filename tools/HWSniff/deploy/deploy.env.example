# Copy to deploy.env and fill in (deploy.env is gitignored).
# Or pass -Target on the command line.

HWSNIFF_PI=sniffer@192.168.1.4
# Optional SSH options, e.g. -i C:\Users\you\.ssh\id_ed25519
# HWSNIFF_SSH_OPTS=
